import React from 'react'
import './App.css'

function Item() {

  return (
    <div className="container">
        {/* {value.name === '' ? 
        'Name: Null Txt: Null' : 
        `Name: ${value.name} 
        Surname: ${value.surname} 
        Txt: ${value.txt} `
        } */}
        asd
    </div>
  );
}

export default Item;
